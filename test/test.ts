import { IItem } from "../src/game"

import { expect } from "chai";
import "mocha";

describe("Total true", () => {
    it("should equal 0 for call with 0", () => {
        expect(0).to.equal(1 - 1);
    });
});
